[build]
  command = "pip install -r requirements.txt"
  functions = "functions"

[functions]
  node_bundler = "esbuild"

[functions.api]
  runtime = "python"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true
